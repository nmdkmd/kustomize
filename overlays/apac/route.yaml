apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: todonodejs
    name: frontend
  name: frontend-apac
  namespace: mysql
spec:
  host: todo.apps.ocp4.exmaple.com
  port:
    targetPort: 8080
  tls:
    termination: edge
  to:
    kind: Service
    name: frontend-apac
    weight: 100
  wildcardPolicy: None
